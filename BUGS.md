# TRAIN — Bug-Tracking
*Wird nach jedem Sprint aktualisiert*
*Stand: Juli 2026 / train-v176*

---

## BEHOBEN

| ID | Beschreibung | Version | Commit | Root Cause |
|----|-------------|---------|--------|-----------|
| B36 | Coach-Tab Push/Pull-Warnung (`_checkPushPullBalance()`, weeklyFocus.js) nutzte noch die alte success-only-Zählung, obwohl die Zwillingsfunktion in ui.js (`_renderMovementPattern()`) seit B32 success+fail zählt | v169 | — | Gefunden im großen Deep-Check-Audit (2 unabhängige Prüf-Durchläufe fanden das übereinstimmend). Die Datei-Kommentar über `_checkPushPullBalance()` behauptet explizit "identisch" zur ui.js-Version — stimmte seit B32 nicht mehr. Fix: `ex.sets.filter(s => s.status === 'success')` → `filter(s => s.status === 'success' \|\| s.status === 'fail')`, exakt wie in ui.js. Verifiziert per Node-Skript (`computeStructuralSignals()` direkt aufgerufen mit synthetischem State: 8 success + 8 fail Push-Sätze/Woche über 4 Wochen ergeben jetzt korrekt 64 Push-Sätze im Ratio-Text, nicht mehr 32). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B37 | Archivierte Übungen wurden weiterhin in `_renderMovementPattern()` (Kategorie-Balken UND Push/Pull-Ratio, ui.js) sowie in `_checkPushPullBalance()` (weeklyFocus.js) mitgezählt | v169 | — | Gefunden im Deep-Check-Audit, empirisch per Playwright bestätigt (Übung archiviert, Sätze blieben in beiden Fortschritt-Tab-Anzeigen sichtbar). `_weekSuccessScore()` (ui.js) hatte bereits korrekt `if (ex.archived) continue;`, die 3 anderen Zählstellen nicht. Fix: identischer Guard in allen 3 betroffenen Schleifen ergänzt (ui.js Kategorie-Balken, ui.js Push/Pull-Ratio, weeklyFocus.js `_checkPushPullBalance()`). Verifiziert per Node-Skript: synthetische archivierte Übung mit 200 Sätzen über 4 Wochen taucht nicht mehr in der Push/Pull-Zählung auf. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B38 | `_weekConsistencyRatio()` (consistencyUtils.js) zählte JEDEN nicht-Rest-Urlaubstag automatisch als "erledigt", unabhängig davon ob tatsächlich trainiert wurde — im Widerspruch zu `_weekTrainingStatus()` (state.js), die für dieselben Tage echte Aktivität (≥1 bewerteter Satz) verlangt | v169 | — | Gefunden im Deep-Check-Audit per Code-Vergleich der beiden eng verwandten Kennzahlen. Auswirkung: ein nie absolvierter "leichter" Urlaubstag zeigte 100% Konsistenz im Fortschritt-Tab-Trend UND in den Coach-Signalen `_checkConsistencyGap`/`_checkConsistencyQuality` (beide nutzen `_weekConsistencyRatio()`), während dieselbe Woche die Streak nicht erhöht hätte. Fix: Urlaubstage zählen jetzt nur noch als erledigt bei `day.markedDone` ODER mindestens einem bewerteten Satz (`success`/`fail`) — nicht mehr automatisch. Verifiziert per Node-Skript: Urlaubstag mit 0 bewerteten Sätzen liefert jetzt korrekt Ratio 0 (vorher 1), mit ≥1 bewertetem Satz weiterhin korrekt 1. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B39 | Undo-Stack: reine Einstellungs-Aktionen (`SETTING_TOGGLE`, `SETTING_SET`, `AUTOWEEK_SET`, `TOGGLE_FAVORITE`) waren nicht in `_NO_UNDO` und konnten den einzigen globalen Undo-Slot belegen | v169 | — | Gefunden im Deep-Check-Audit. Konkretes Szenario: Nutzer löscht versehentlich eine Übung → tippt kurz danach (auch aus Versehen) einen Einstellungs-Schalter an → "Rückgängig" macht den Schalter rückgängig, nicht die Löschung — die Löschung ist dann endgültig verloren. Fix: alle 4 Actions zu `_NO_UNDO` (state.js) hinzugefügt, analog zu bereits vorhandenen Einträgen wie `SET_ERKENNTNISSE_HORIZONT`. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B40 | Gewichtsempfehlung (`_recommendationCore()`, weightRecommendation.js): RPE-Bereich 7.0–7.5 wurde von keiner Steigerungs-Bedingung abgedeckt (führte immer zu "halten"), UND bei identischer Erfolgsquote konnte ein NIEDRIGERES (besseres) RPE eine schlechtere Empfehlung ergeben als ein höheres — die "niedriges RPE"-Bedingung verlangte eine strengere Erfolgsquote (≥0.9) als die "gute Form"-Bedingung (≥0.8) für ein schlechteres RPE | v169 | — | Gefunden im Deep-Check-Audit, mit Nutzer besprochen (2 Optionen: nur dokumentieren vs. begradigen — Nutzer wählte "begradigen"). Fix: Grenze der "niedriges RPE"-Bedingung von `avgRpe <= 7` auf `avgRpe <= 7.5` erweitert (schließt die Lücke exakt an der Untergrenze der "gute Form"-Bedingung, die weiterhin bei `avgRpe > 7.5` beginnt) und deren Erfolgsquoten-Schwelle von `>= 0.9` auf `>= 0.8` gesenkt (identisch zur "gute Form"-Bedingung) — niedrigeres RPE kann jetzt bei gleicher Erfolgsquote nie mehr eine schlechtere Empfehlung als höheres RPE ergeben. Betrifft NICHT die separate, in DECISIONS.md dokumentierte "RPE-Schwellen bewusst unterschiedlich"-Entscheidung (die gilt für `isReadyForAutoSelect()`s 8.0-Schwelle und die Konfidenz-Einstufung HIGH/MEDIUM in weeklyFocus.js — andere Funktionen, unangetastet). Verifiziert per Node-Skript: RPE 7.0/85% liefert jetzt `delta:2.5` (vorher `0`), RPE 8.0/85% weiterhin `delta:1.25` — RPE 7.0 ist jetzt nie mehr schlechter gestellt als RPE 8.0 bei gleicher Erfolgsquote. RPE 7.2/85% (vorher in der toten Lücke) liefert jetzt korrekt `delta:2.5` statt `0`. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B44 | Dritte unabhängige Implementierung von "welche Tage einer Woche zählen als geplant/fällig" (`_reachableDays()` in weekReview.js) — filterte NICHT über `isTrainingDay()`, im Unterschied zu `_weekConsistencyRatio()` (consistencyUtils.js) und `_weekTrainingStatus()` (state.js) | v170 | — | Gefunden bei der echten Geräte-Verifikation von B38 (2026-07-14). Nutzer bat um eine systematische Konsolidierungs-Prüfung des ganzen Codes auf dieses Muster statt nur diesen einen Fall zu fixen — siehe B45/B46 für die weiteren Funde desselben Audits. Fix: `weekReview.js` importiert jetzt `isTrainingDay()` aus state.js (reine, zustandslose Prädikat-Funktion — kein `getState()`/`dispatch()`, daher kein Widerspruch zum Datei-Kopfkommentar "kein State-Zugriff", der entsprechend präzisiert wurde) und filtert `_reachableDays()` damit, identisch zu den beiden anderen Implementierungen. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B45 | `_calcSuccessScore()` (weekReview.js, speist "X% Ziel" in der Wochenrückblick-Karte) und `_weekSuccessScore()` (ui.js) berechneten dieselbe Formel (success/(success+fail)) unabhängig voneinander — `_calcSuccessScore()` schloss archivierte Übungen dabei NICHT aus, `_weekSuccessScore()` schon | v170 | — | Gefunden im Konsolidierungs-Audit nach B44 (2026-07-14) — derselbe Fehlertyp wie B37, an einer bis dahin ungeprüften vierten Stelle. Fix: neue gemeinsame Funktion `weekSuccessCounts(week)` in setUtils.js (bereits die "zentrale Helfer"-Datei für `isFullSuccess()`) — sowohl `_weekSuccessScore()` (ui.js) als auch `_calcSuccessScore()` (weekReview.js) delegieren jetzt dorthin, keine zwei Kopien der Formel mehr. Verifiziert per Node-Skript: archivierte Übung mit 3 fail-Sätzen fließt nicht mehr in `weekSuccessCounts()` ein. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B46 | Bewegungskategorie-Lookup (`customCatMap[name] ?? MOVEMENT_MAP[name]`) 2x identisch dupliziert (ui.js, weeklyFocus.js) UND komplett fehlend in `computeBreadthProgress()` (overallPerformance.js, Fortschritt-Tab "Breite"-Kennzahl) — dort wurden Kategorie-Overrides (`EX_SET_CATEGORY_OVERRIDE`) schlicht ignoriert | v170 | — | Gefunden im Konsolidierungs-Audit nach B44 (2026-07-14). Fix: neue Funktionen `buildCategoryMap(customExercises)` + `resolveCategory(name, categoryMap)` in movementMap.js (der ohnehin bereits von allen 3 betroffenen Dateien importierten Datei) — alle 3 Stellen nutzen jetzt dieselben zwei Funktionen statt eigener Inline-Logik bzw. gar keiner. Verifiziert per Node-Skript: eine per Override auf 'Hinge' umkategorisierte Kniebeuge (Standard-Kategorie eigentlich 'Squat') erscheint jetzt korrekt als "Hinge" in `computeBreadthProgress()`-Ergebnissen, vorher wäre sie fälschlich als "Squat" gezählt worden. Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B47 | PR-Tracking (`state.prs`, `ex.oneRM`, `ex.prWeight`/`prRepsAtMaxWeight`/`prRepsHistory`) war 3x unabhängig implementiert (`SET_TOGGLE_DONE`, `CONFIRM_SET`, `AUTO_EVAL_SET` in state.js) — `CONFIRM_SET` und `AUTO_EVAL_SET` waren wortwörtlich identisch, `SET_TOGGLE_DONE` (der manuelle ✓-Button, die HÄUFIGSTE Eingabeart) hatte aber das `ex.oneRM`-Update komplett vergessen | v170 | — | Gefunden bei der genaueren Prüfung von Fund 4 aus dem Konsolidierungs-Audit (2026-07-14, war zuvor bewusst zurückgestellt statt spekulativ konsolidiert). Zeile-für-Zeile-Vergleich aller 3 Blöcke bestätigte: `CONFIRM_SET`/`AUTO_EVAL_SET` sind bit-identisch (nur kosmetische Whitespace-Unterschiede), `SET_TOGGLE_DONE` fehlte der `ex.oneRM`-Block komplett — ein ECHTER Bug, nicht nur Duplikations-Risiko. Auswirkung: der Trainings-Tab-1RM-Hinweis (ui.js:1606) hat zwar einen Live-Fallback (rechnet bei fehlendem `ex.oneRM` direkt aus den aktuellen Sätzen), der das kaschiert — aber `ex.oneRM` ist als wochenübergreifendes historisches Maximum gedacht (`_resetClonedDays()` rührt es beim Wochenwechsel bewusst nicht an, siehe Migration v19→v20-Kommentar), während der Fallback nur die Sätze der AKTUELLEN Woche kennt. Nutzer, die nur über den ✓-Button bestätigen, sahen den Hinweis in einer neuen, noch leeren Woche schlicht verschwinden, statt weiterhin den Bestwert aus der Vorwoche zu zeigen. Fix: neue gemeinsame Funktion `_applyPrTracking(state, ex, weight, reps)` in state.js — alle 3 Reducer delegieren jetzt dorthin, keine 3 Kopien mehr. Verifiziert per Node-Skript mit echtem `dispatch(A.SET_TOGGLE_DONE, ...)` (nicht nur die extrahierte Funktion isoliert getestet): `ex.oneRM` war vorher `null`, ist nach dem Fix korrekt `116.7` (Epley: 100kg × (1+5/30)). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B48 | Coach-Gewichtsempfehlung (`getWeightRecommendation()`, weightRecommendation.js) ignorierte die pro Übung eingestellte Schrittweite (`ex.weightStep`) bei der eigentlichen Sprunggröße — sie war fest auf 2.5kg ("volle Steigerung") bzw. 1.25kg ("kleine Steigerung") hartkodiert, unabhängig davon, was für die jeweilige Übung eingestellt war. Die Schrittweite wurde nur zum Runden des Ergebnisses benutzt, nicht zur Bestimmung des Sprungs selbst | v172 | — | Vom Nutzer gemeldet: "technisch funktioniert es, aber nicht mit der gewünschten Logik" — bei schweren Grundübungen (Kniebeuge, Kreuzheben) macht ein 5kg-Sprung Sinn, bei Bankdrücken eher 1.25kg, das eingestellte Schrittweite-Feld pro Übung existierte bereits (Schrittweite-Picker in der Übungskonfiguration, auch vom manuellen "+kg"-Button bereits korrekt genutzt), wurde aber von der AUTOMATISCHEN Coach-Empfehlung ignoriert. Konkrete Auswirkung: bei einer 5kg-Schrittweite-Übung konnte eine "kleine Steigerung" (intern 1.25) auf die nächste 5kg-Stufe runden und dabei zufällig auf +0kg fallen (keine sichtbare Änderung trotz "Steigerung möglich"-Text). Fix: `fullDelta`/`halfDelta` in `getWeightRecommendation()` werden jetzt aus der pro Übung eingestellten Schrittweite abgeleitet (`fullDelta = weightStep`, `halfDelta = weightStep/2`) — identisch zum Muster, das `getMetricRecommendation()` (Distanz/Zeit-Übungen) schon immer nutzt. Nutzer-Entscheidung: bei bereits kleiner Schrittweite (≤1.25kg, kleinste gängige Hantelscheibe) bleibt "kleine Steigerung" bei 1× Schrittweite statt weiter zu halbieren (0.625kg wäre nicht auflegbar). Rückwärtskompatibel: Standard-Schrittweite 2.5kg liefert weiterhin exakt +2.5/+1.25 wie vorher. Zusätzlich 3 Insight-Trigger-Stellen in insightEngine.js (A-01/A-01b/A-02, Toast-Vorschläge) korrigiert — übergaben bisher `undefined` statt der Übungs-Schrittweite, fielen also auf denselben pauschalen 2.5kg-Default zurück. Verifiziert per Node-Skript: Kniebeuge (Schrittweite 5) liefert jetzt +5/+2.5 (voll/klein), Bankdrücken (Schrittweite 1.25) liefert +1.25/+1.25 (keine Halbierung), Standard-Schrittweite (2.5) liefert weiterhin +2.5/+1.25 (Alt-Verhalten exakt erhalten). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B49 (Feature) | Schrittweite-Vorschlag aus geloggter Historie: erkennt, wenn eine Übung wiederholt um denselben Betrag gesteigert wurde, und zeigt das als sichtbaren, NIE automatisch angewendeten Vorschlag neben der Schrittweite-Einstellung | v173 | — | Anschluss an B48 — Nutzer fragte, ob die App automatisch erkennen kann, welche Schrittweite pro Übung sinnvoll ist ("höchst individuell"), statt sie manuell pflegen zu müssen. Bewusst KEINE stille Automatik (Nordstern "Decision Support") — nur ein Hinweis mit "Übernehmen"-Button, identisch zum bereits etablierten `.target-suggestion`-Muster (targetReps-Vorschlag). Neue Funktionen `detectRecurringStep()`/`exMetricHistory()`/`detectRecurringWeightStep()` (insightEngine.js) — Schwelle: mindestens 3 identische positive Wochen-Sprünge bei ≥4 dokumentierten Wochen (Analogie zu plateauDetector.js's `plateauWeeks=3`). Gilt einheitlich für Gewicht- UND Distanz/Zeit-Übungen. Dispatcht die bereits existierende `EX_SET_STEP`/`EX_SET_METRIC_STEP`-Action, keine neue Action nötig. Verifiziert per Node-Skript (Muster-Erkennung: 3× identischer Sprung → erkannt, wechselnde Deltas → nicht erkannt) und per Playwright (Vorschlag erscheint mit korrektem Text, "Übernehmen" setzt die Schrittweite, Vorschlag verschwindet danach). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B50 (Feature) | Anpassbare Steigerungsmenge im "Neue Woche"-Empfehlungs-Chip: war bisher reines Ein/Aus (genau der vorgeschlagene Wert oder nichts), jetzt zusätzlich freie Eingabe eines eigenen Werts ("Anderer Wert") | v173 | — | Nutzer-Beispiel: "App schlägt +5kg vor, ich traue mir aber nur +2,5kg zu". Bewusst KEIN festes Halbierungs-Preset (Entscheidung mit Nutzer abgestimmt) — würde mit dem bereits internen `halfDelta`-Konzept aus B48 kollidieren und deckt ohnehin nicht jeden Wunschwert ab; freie Eingabe (wiederverwendet das bestehende `.ex-kg-picker`-Muster vom In-Session-"+kg"-Button) ist einfacher und flexibler. **Kritisches Architektur-Risiko gefunden und gelöst:** `_prepNewWeekModal()` dispatcht bei JEDEM Re-Render erneut die Auto-Vorauswahl — ein selbst gewählter Custom-Wert wäre ohne Gegenmaßnahme beim nächsten Re-Render (z.B. "Weitere anzeigen"-Klick) stillschweigend wieder auf den vollen Empfehlungswert zurückgesprungen. Fix: neues Tracking `_userCustomStepChoice` (Map exName→Wert), analog zum bestehenden `_userDismissedAutoSelect`-Muster, das `autoSelected`/`alreadyConfirmedSame` in `_prepNewWeekModal()` entsprechend erweitert. Gilt einheitlich für Gewicht- UND Distanz/Zeit-Übungen (derselbe Chip-Code). Verifiziert per Playwright (Kernszenario exakt nachgestellt: Chip zeigt "+5kg empfohlen" automatisch vorausgewählt → "Anderer Wert" → 2.5 eingeben → bestätigen → Chip zeigt "+2.5kg (angepasst)" → **Re-Render erzwungen → Chip zeigt weiterhin +2.5kg, NICHT zurückgesprungen auf +5kg** → Woche erstellt → tatsächliches neues Gewicht ist 102.5kg, nicht 105kg). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B51 (Feature) | Pre-Launch-Checkliste (1/3): Google Fonts liefen live über `fonts.googleapis.com`/`fonts.gstatic.com` — der einzige externe Netzwerk-Aufruf der gesamten App, widersprach direkt der eigenen "kein Server/Datenschutz als USP"-Positionierung (DECISIONS.md) und ist in Deutschland ein bekanntes DSGVO-Abmahnrisiko (LG München 2022: IP-Übertragung an Google ohne Einwilligung) | v174 | — | Vor dem Launch an die ersten ~20 Nutzer systematisch nach Punkten gesucht, die einen schlechten ersten Eindruck oder ein echtes Risiko verursachen könnten (Nutzer-Anfrage). Fix: Bebas Neue + DM Sans (4 woff2-Dateien, identisch zu den bisher von Google ausgelieferten) selbst gehostet unter `fonts/`, `@font-face`-Regeln in styles.css (DM Sans als Variable-Font-Range 300-600, ein File statt 4 Duplikaten), Google-Fonts-Links aus index.html entfernt. sw.js: dediziertes StaleWhileRevalidate/FONT_CACHE für Google-Fonts-Runtime-Caching komplett entfernt (jetzt totes Feature, da Fonts same-origin sind und über den normalen App-Shell-Precache laufen) statt als toten Code stehen zu lassen. Verifiziert: `grep -rn "https://"` über alle .js/.html-Dateien zeigt danach keinen einzigen externen Aufruf mehr (außer dem bewusst gewählten GoatCounter, siehe B52); Playwright bestätigt beide Font-Familien laden fehlerfrei (`document.fonts` Status 'loaded', 0 failed requests). |
| B52 (Feature) | Pre-Launch-Checkliste (2/3): keine Möglichkeit zu sehen, wie viele Nutzer die App tatsächlich (regelmäßig) verwenden — die App läuft rein statisch auf GitHub Pages, es gab keinerlei Backend/Tracking. Zusätzlich: kein globaler JS-Fehler-Handler (Crashes blieben für Nutzer stumm und für den Betreiber unsichtbar) und kein Feedback-Kanal für Bug-Reports | v174 | — | Mit Nutzer abgestimmt: GoatCounter (kostenlos, Open Source, cookielos, DSGVO-konform ohne Consent-Banner, unterstützt Custom Events) statt Google Analytics o.ä. — zählt ausschließlich anonyme Nutzungs-Events, nie echte Trainingsdaten. Script-Tag in index.html mit Platzhalter-Site-Code (`<SITE-CODE>`, MUSS vor Launch durch echten GoatCounter-Account ersetzt werden). Neue Custom-Events an bestehenden Dispatch-Punkten angehängt (kein Reducer-Umbau): "Woche erstellt" (`_createWeek()`, ui.js) und "Onboarding abgeschlossen" (`_finish()` im Onboarding-Flow). Neuer globaler `window.addEventListener('error'/'unhandledrejection', ...)` in index.html feuert ein `train:js-error`-Browser-Event, auf das ui.js mit einem Toast ("Etwas ist schiefgelaufen") UND einem anonymen GoatCounter-Event (`js_error`, nur Fehlermeldung, keine Nutzdaten) reagiert — reused das bestehende `train:show-update-banner`-Event-Muster (Service-Worker-Update-Banner) statt einen neuen Kommunikationsweg zu erfinden. Feedback-Kanal: neue "Feedback"-Zeile in den Einstellungen (Info-Sektion) mit `mailto:`-Link (Platzhalter-Adresse, MUSS vor Launch ausgefüllt werden). Verifiziert per Playwright: synthetisches `train:js-error`-Event zeigt den erwarteten Toast-Text zuverlässig an. |
| B53 (Feature) | Pre-Launch-Checkliste (3/3): (a) Info-Sektion in den Einstellungen zeigte eine seit langem eingefrorene, falsche Versionsnummer ("TRAIN v9.0" statt train-v174) — bei Bug-Reports von Nutzern nicht mehr zuordenbar; kein Impressum/Datenschutzerklärung vorhanden (bei einem öffentlich erreichbaren Angebot an mehrere fremde Nutzer vermutlich nach §5 TMG erforderlich). (b) Zusätzlich unabhängig gefunden beim vollen Lighthouse-Lauf: `manifest.json` hatte GAR KEIN `icons`-Array, und die von index.html referenzierten `icon-192.png`/`icon-512.png` existierten im gesamten Projekt nirgends — "Zum Home-Bildschirm hinzufügen" hätte keinerlei App-Icon gezeigt (schlechtester denkbare erste Eindruck für eine PWA). Außerdem fehlender `<link rel="icon">` (Browser fragt automatisch `/favicon.ico` an → 404, von Lighthouse als Best-Practices-Fehler markiert) | v174 | — | (a) Info-Sektion (ui.js `renderSettingsTab()`) erweitert: Versionszeile korrigiert, neue aufklappbare "Datenschutz"-Zeile (erklärt: Trainingsdaten bleiben lokal, GoatCounter zählt nur anonym, Fonts selbst gehostet) und "Impressum"-Zeile (Platzhaltertext, MUSS vor Launch mit echten Kontaktdaten gefüllt werden) — reused das bereits bestehende `.session-note-toggle`/`.session-note-body`-Akkordeon-Muster (bisher nur für Aufwärmen/Cooldown-Notizen genutzt) statt neue CSS-Klassen zu erfinden. (b) Neue Platzhalter-Icons generiert (dunkler Hintergrund + "TRAIN"-Wortmarke in Akzent-Lime, identisch zum bestehenden Splash-Screen-Branding aus index.html, per Playwright-Screenshot einer gerenderten HTML-Vorlage erzeugt — kein fertiges Icon-Design, sollte vor/nach Launch ggf. durch ein poliertes Icon ersetzt werden), `manifest.json` um `icons`-Array ergänzt, `<link rel="icon">` in index.html ergänzt, beide PNGs zur sw.js-Precache-Liste hinzugefügt. Voller Lighthouse-Lauf (alle Kategorien, vorher nur gezielt Accessibility geprüft): Accessibility 100, Best-Practices 100 (vorher 96, durch Favicon-Fix behoben), SEO 100, Performance ~57-60 (Lighthouses simulierte "Slow-4G"-Mobilthrottling, 150ms RTT/~1.6Mbps — Ursache ist die bewusste "kein Bundler"-Architektur, siehe CLAUDE.md: ~20 einzelne ES-Module müssen unter dieser simulierten Drosselung nacheinander/parallel angefragt werden, bevor der erste Frame rendert. `modulepreload`-Hints für alle App-Module in index.html ergänzt als risikofreie Optimierung — Verbesserung lag innerhalb der Mess-Rauschgrenze, kein spürbarer Effekt, aber unschädlich und strukturell korrekt beibehalten. Ein Bundler/Build-Schritt wäre die einzige echte Abhilfe, aber bewusst NICHT umgesetzt — das wäre eine offene Architektur-Änderung außerhalb des Scopes dieser Checkliste. Reale Nutzer mit normalem WLAN/LTE werden vermutlich deutlich bessere Ladezeiten erleben als dieses simulierte Worst-Case-Szenario. |
| B59 | XSS-Lücke im Template-Editor: `ex.name` wurde an einer Stelle ohne `h()`-Escaping direkt in ein `value`-Attribut geschrieben — sowohl beim Tippen als auch beim Importieren einer fremden Backup-Datei ausnutzbar | v176 | — | Ausgelöst durch Nutzer-Anfrage nach einer Security-Bestandsaufnahme vor dem Public-Launch (Instagram-Beispiele "vibecoded" Apps mit Sicherheitslücken). TRAIN hat kein Backend/keine API-Keys/keine Accounts (verifiziert: kein `fetch()` außer Service-Worker-Caching, keine Secrets im Repo) — der Großteil der üblichen Checkliste (Rate Limiting, Auth, SQL-Injection, SSRF, IDOR) läuft daher ins Leere. Einzig real gefundene Lücke: `ui.js:4134` (`renderTemplateEditor()`) schrieb `value="${ex.name}"` ohne das im Rest der Codebase konsequent verwendete `h()` (ui.js:306) — ein Übungsname wie `x"><img src=x onerror=...>` brach aus dem Attribut aus und führte beim Öffnen des Template-Editors automatisch aus. Zusätzlich über `backup.js`s JSON-Import erreichbar: die Validierung prüfte nur `weeks`-Array/`meta.schemaVersion`/`settings`-Objekt, keine Typ-/Längenprüfung einzelner Textfelder — eine geteilte, präparierte `TRAIN_Backup.json` (z.B. "Trainingsplan-Vorlage" aus einem Forum) konnte den Payload direkt in `state.customTemplate` einschleusen, unabhängig vom UI-seitigen `maxlength="80"`. **Fix:** (1) `ui.js:4134` → `h(ex.name)`, konsistent zum bereits etablierten Muster (z.B. ui.js:1533). (2) `backup.js`: neue `_sanitizeImportedState()` (Defense-in-Depth, ergänzt das Escaping in ui.js, ersetzt es nicht) — normalisiert `name`/`note`/`title`/`subtitle` in `weeks[].days[].exercises[]`, `weeks[].days[]` und `customTemplate[]` auf String-Typ (sonst `''`) mit Längen-Deckel (80/200 Zeichen), plus 5-MB-Obergrenze für die importierte Datei selbst (`file.size`-Check vor dem Lesen). (3) `index.html`: CSP-`<meta>`-Tag ergänzt (`default-src 'self'`, Skripte nur `'self'`+GoatCounter, `object-src 'none'`, `frame-ancestors 'none'`) als zusätzliche Absicherung — nur per `<meta>` möglich, da GitHub Pages keine echten HTTP-Header erlaubt; `'unsafe-inline'` bei `script-src` bleibt nötig für den bestehenden Bootstrap-`<script>` und 4 bestehende inline-`onclick`-Handler (Notiz-Toggle), die ohne Build-Step nicht per Nonce/Hash absicherbar sind. Alle anderen unescaped `${...}`-Stellen in ui.js (Badge-Titel, Onboarding-Templates) wurden geprüft und sind feste Konstanten, kein Nutzertext — kein weiterer Fund. **Verifiziert:** neuer Regressionstest `tests/security_xss.spec.js` — Payload per JSON-Import eingeschleust, Template-Editor geöffnet, bestätigt `window.__xssFired` bleibt `false` und kein `<img src="x">` im DOM; Test manuell gegen den unreparierten Code laufen lassen (bestätigt: schlägt ohne den Fix erwartungsgemäß fehl, danach wieder behoben). Volle Suite `npx playwright test`: 19/19 grün (10/10 regression_core, 17 fixtures, neuer Security-Test), inkl. Bestätigung dass die neue CSP GoatCounter/Coach-Toasts/Tab-Navigation nicht bricht. Phase-2-Blaupause (Auth/Rate-Limiting/Access-Control-Matrix/API-Key-Handling — erst relevant sobald die geplante Paywall/Coaching-Funktion einen Server bekommt) dokumentiert in `SECURITY.md`. |
| B54 (Feature) | Onboarding: kein Weg, die App direkt zum Home-Bildschirm hinzuzufügen — Nutzer musste das manuell über Browser-Menüs finden | v175 | — | Nutzer fragte im Anschluss an die Pre-Launch-Checkliste, ob sich das automatisieren lässt. Technischer Befund: auf Android/Chrome/Edge existiert `beforeinstallprompt` (echter Ein-Tap-Install-Dialog auslösbar), auf iOS Safari gibt es KEINE solche API (Apple erlaubt das nicht) — dort ist nur eine Anleitung möglich, kein echter Auto-Trigger. Umsetzung: index.html fängt `beforeinstallprompt` global ab (`preventDefault()` + auf `window.__trainInstallPrompt` gespeichert, verhindert die browsereigene Mini-Infobar zugunsten der eigenen UI) und feuert bei `appinstalled` ein `train:app-installed`-Event (→ anonymes GoatCounter-Event "App installiert", aussagekräftigeres Signal als reine Seitenaufrufe). Neuer Onboarding-Screen (`_obPhase='install'` in `_showOnboarding()`, ui.js) erscheint NACH der Vorlagen-Wahl (nicht davor — Best Practice: erst Wert zeigen, dann installieren fragen), aber NUR wenn er auch etwas bewirken kann: iOS (Anleitung) ODER ein eingefangenes `beforeinstallprompt` liegt vor. Auf nicht unterstützten Plattformen (Desktop-Firefox, bereits installiert — `display-mode:standalone`/`navigator.standalone` geprüft) wird der Screen übersprungen statt einen wirkungslosen Button zu zeigen. Reused bestehende Muster: `.ob-*`-Klassen (teils bisher ungenutzte CSS wie `.ob-logo`/`.ob-sub` erstmals bespielt), `train:show-update-banner`-Event-Stil für die index.html↔ui.js-Kommunikation. Verifiziert per Playwright in 3 Szenarien (simuliertes `beforeinstallprompt` → Button ruft `prompt()` auf; iOS-User-Agent → Anleitung erscheint nach Tap; weder/noch → Screen wird komplett übersprungen). Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B01 | Auto-Eval bei 0 Wdh → Satz bleibt pending | v151 | f1d4f54 | Guard `reps <= 0` in _handleBlur → geändert zu `reps < 0` |
| B02 | Wochenerstellung: Erfolgs-Toast obwohl nichts erstellt | v151 | f1d4f54 | before/after weeks.length Check fehlte im _createWeek() Handler |
| B03 | Verpasste Einheiten in laufender Woche | v152 | 66c034d | _reachableDays(): `<=` statt `<` — heutiger Tag galt als bereits fällig |
| B04 | Zeitbezug zeigt "13 Wochen" obwohl App jünger | v151 | f1d4f54 | effectiveN nicht gegen realWeeks geclampt |
| B05 | Fulfill-Meter zählt fail-Sätze nicht | v151 | f1d4f54 | Filter nur success, fail-Sätze wurden ignoriert |
| B06 | Wiedereinstieg feuerte 14 Tage → zu lang | v151 | f1d4f54 | REENTRY_WINDOW_DAYS = 14 → 7 |
| B07 | _weekConsistencyRatio zählt future-Tage | v153 | ab33633 | `<=` statt `<` — identischer Off-by-one wie B03 |
| B08 | ui.js:1183 Live-Hinweis inkonsistent zu Fulfill-Meter | v154 | 8686458 | 1183 nur success, 1642 success+fail seit v151 |
| B09 | Prozentzahlen: s.reps als String → Milliarden-% | v143 | — | s.reps wurde als String gespeichert, parseFloat fehlte |
| B10 | weightStep=0 → NaN in Gewichtsempfehlung | v143 | — | `?? 2.5` statt `|| 2.5` — 0 ist falsy für ?? nicht |
| B11 | Erkenntnisse-Stepper springt zum Übungsfortschritt | v152 | 66c034d | Layout-Reflow nach innerHTML-Replace, scrollTop-Restore fix |
| B12 | Erkenntnisse-Stepper: Plus über echte Wochen hinaus | v152 | 66c034d | Kein stepperMax gegen realWeeks |
| B13 | barbell Row nicht als Pull erkannt | v149 | 8143086 | "Barbell Row" fehlte in movementMap.js |
| B14 | Pull Ups (weight=0) triggert _checkRisingRpe nicht | — | — | Guard weights.some(w=>w===0) — by design, kein Bug |
| B15 | isSeedWeek als Vorlage für neue Woche | v151 | f1d4f54 | _templateWeekForNewWeek() überspringt isSeedWeek |
| B24 | dragdrop.js nie aktiviert → Touch-Drag defekt auf iOS/Android | v156 | a3752f8 | Polyfill in index.html verdrahtet — `<script src="./dragdrop.js">` + `MobileDragDrop.polyfill()` vor dem Module-Script. Abweichung von der Sprint-Vorlage: touchmove-preventDefault() nur während aktivem Drag (Flag über dragstart/dragend/drop), nicht global — eine bedingungslose preventDefault() hätte jegliches Scrollen auf Touch-Geräten dauerhaft blockiert. Headless bestätigt: Skript lädt fehlerfrei, kein Uncaught-Error, `#app` erreicht `is-ready`. **Update 2026-07-13: echte Geräte-Verifikation zeigt Touch-Drag-Reorder funktioniert weiterhin NICHT** (Übung lässt sich per Long-Press+Drag nicht neu anordnen) — siehe B27, zurückgestellt. |
| B22 | Erfolgsquote inkonsistent (3 verschiedene Formeln im UI) | v157 | e0b0f01 | success/(success+fail) vereinheitlicht, pending ausgeschlossen — als einzig korrekte Semantik (identisch zu `_weekSuccessScore()`) festgelegt. `_getDayCompletionStats()` (ui.js:6419): vorher `successSets/totalSets` (totalSets inkl. pending) → jetzt `successSets/(successSets+failSets)`, `pct:null` statt `0` wenn noch nichts bewertet. `_renderMovementPattern()` (ui.js:2302): Kategorie-Balken zählten vorher nur success-Sätze in Zähler UND Nenner (fail unsichtbar) → jetzt success+fail. `_weekSuccessScore()` bewusst unverändert gelassen (war bereits korrekt). Bewusst NICHT angefasst: Push/Pull-Ratio-Block in `_renderMovementPattern()` zählt weiterhin nur success — out of scope für diesen Sprint, potenzieller Folge-Fund. |
| B23 | Beinbeuger (+ englische Synonyme) unter Squat statt Hinge | v157 | e0b0f01 | movementMap.js korrigiert: `'Beinbeuger'`, `'Leg Curl'`, `'Leg Curls'`, `'Hamstring Curl'` von Squat → Hinge (Hamstring-Curl ist hüftdominant, nicht kniedominant). Zusätzlicher Fund bei der Gelegenheit (nicht angefordert, aber eindeutig): `'Butterfly'` stand unter Pull, korrigiert zu Push — Widerspruch zu den bereits korrekt als Push klassifizierten `'KH Flys'`/`'Flys Kabel'` (Brust-Fly-Bewegung, nicht zu verwechseln mit `'Reverse Flys'`, korrekt unter Pull für hintere Schulter). Geprüfte, bewusst nicht geänderte Grenzfälle (Ausfallschritte/Lunges als Squat, Wadenheben als Hinge, Front/Lateral Raise als Pull, Battle Ropes/Burpees als Core) siehe Sprint-Notiz in HANDOFF.md. |
| B16 | iOS Doppelklick-Zoom beim Picker | v158 | e312751 | **Diagnose korrigiert/erweitert** — die ursprüngliche Notiz "font-size < 16px → Browser-Zoom" beschreibt tatsächlich ein anderes Phänomen (iOS' Zoom-bei-Fokus, ein Einzeltipp-Effekt) als der Bugname nahelegt (Doppeltipp-Zoom). Zwei unabhängige, echte Ursachen gefunden und beide behoben: (1) Der "+kg"/"+Wdh"-Button (`data-action="inc-weight"`, ui.js:1494) hat eine selbstgebaute 400ms-Doppeltipp-Erkennung (ui.js:4877-4917: einmal tippen = Steigerung bestätigen, zweimal tippen = Werte-Picker öffnen), aber kein `touch-action`, wodurch iOS' natives Doppeltipp-Zoom-Gesture gleichzeitig feuern kann — Fix: `touch-action: manipulation` auf `.btn-icon--kg` (styles.css). (2) `.num-input` (Basis-Klasse für alle Gewicht/Wdh/RPE-Sätze, vorher 14px) und `.ex-kg-picker-custom .num-input` (Custom-Wert-Feld im Picker-Popover, vorher 13px, Override entfernt) lagen unter 16px → Fix: 16px. Grid-Layout-Regression durch die Font-Size-Erhöhung geprüft und ausgeschlossen (A/B-Screenshot bei 390px-Breite, 14px vs. 16px identisch abgeschnitten am rechten Rand — vorbestehend, nicht neu). Regressionstest 10/10 grün nach beiden Fixes. **Update 2026-07-13: echte Geräte-Verifikation bestanden** — Doppeltipp auf +kg/+Wdh löst kein Zoom mehr aus, Fokus auf Set-Eingabefeld löst kein Zoom mehr aus. Beide Ursachen final bestätigt behoben. |
| B25 | Coach zeigt "Auf Kurs" trotz mehrwöchigem komplettem Fail | v160 | 668b00a | Gefunden bei Edge-Case-Audit v159 (TRAIN_Test_EdgeCase_AllesFail_GuterSchlaf.v1.json). Root Cause: JEDE Funktion in der computeWeeklyFocus()-Kaskade filterte auf `status === 'success'`, bevor sie ein Signal berechnete — bei 0 Erfolgen fiel alles bis zum Fallback "Auf Kurs" durch. Produktentscheidung getroffen (Design-Diskussion mit Nutzer): neue Funktion `_checkPersistentFailure()` (weeklyFocus.js), Priorität 2 direkt nach `_checkReentry` und VOR `_checkOverload` (eingetretenes Totalversagen dringlicher als drohende Überlastung), Schwelle 0% Erfolg über 3 Wochen (analog zum 3-Wochen-Fenster von `_checkPlateau`), muss in allen 3 Wochen tatsächlich versucht+bewertet worden sein. Empfehlung berechnet konkretes Gewicht via bestehendem `deloadFactor`+`roundToPlate()` (z.B. "Gewicht bei Bankdrücken auf ~60 kg reduzieren" statt nur Text). Neues Icon 🛑 (`persistent_failure`) in ui.js `_FOCUS_ICONS` ergänzt. Bewusst OHNE Decisional-Balance (buildDecisionalBalance() unterstützt nur 'overload'/'consistencyGap') — UI zeigt in dem Fall einfach keine Stay/Change-Buttons, das ist bereits das bestehende Fallback-Verhalten für Status ohne Balance (z.B. plateau/progression). Verifiziert: beide AllesFail-Fixtures zeigen jetzt korrekt "🛑 Gewicht zu hoch" statt "Auf Kurs" bzw. statt "Schlaf priorisieren" (persistent_failure hat jetzt Vorrang, wie entschieden). Regressionstest 10/10 grün. |
| B17 | Heute anders — kein frisches Template | v159 | 6e1a203 | Root Cause (Korrektur einer eigenen Fehldiagnose vom Edge-Case-Audit v158, die "positionsbasiert" behauptet hatte — tatsächlich namensbasiert, aber auf den FALSCHEN Namen): `renderExercise()` sucht `prevEx` bei einer Ausweichübung bewusst über `ex.substituteFor` statt `ex.name` (ui.js:1217, für den Fulfill-Meter-Metrik-Check bei Zeile 1635 sinnvoll) — dieser `prevEx` wurde aber auch für die "Vorwoche"-Adopt-Hints in `renderSetRow()` verwendet und zeigte dadurch Gewicht/Wdh der URSPRÜNGLICHEN Übung (z.B. Kniebeuge: 100kg) als Vorschlag für die tatsächlich heute ausgeführte Ausweichübung (z.B. Beinpresse, die noch nie trainiert wurde) — potenziell irreführend, da unterschiedliche Übungen meist sehr unterschiedliche Gewichtsbereiche haben. Fix: in `renderSetRow()` wird `prevSet` jetzt explizit auf `null` gesetzt wenn `ex.substituteFor` gesetzt ist (ui.js:1667) — unterdrückt nur die Adopt-Hints, lässt `prevEx` für den unabhängigen Fulfill-Meter-Guard unangetastet. Verifiziert mit TRAIN_Test_HeuteAnders.v1.json: `adopt-prev-weight`/`adopt-prev-reps`-Buttons erscheinen jetzt korrekt nicht mehr für die Ausweichübung, Sub-Banner weiterhin korrekt. Regressionstest 10/10 grün. |
| B26 | buildDecisionalBalance() fehlte für persistent_failure | v161 | 48b7272 | Nach Design-Diskussion (siehe DECISIONS.md) `_balanceForPersistentFailure()` in weeklyFocus.js ergänzt und in `buildDecisionalBalance()` eingehängt. "Gewicht reduzieren (Empfehlung)" dispatcht `EX_SET_NEXT_WEEK_PLAN` für die betroffene Übung (di/ei per Name in der aktuellen Woche gesucht, identisches Muster wie die Plateau-deload-Strategie) mit Delta `-(currentWeight * (1 - deloadFactor))`, gerundet auf weightStep — bewusst über `deloadFactor` statt Plateaus eigenem hartkodierten 22.5%-Wert, damit die im Coach-Text angezeigte "~X kg"-Empfehlung exakt zum tatsächlich gesetzten Wert passt. `_checkPersistentFailure()` liefert jetzt zusätzlich `currentWeight`/`suggestedWeight` mit, damit ui.js sie nicht neu berechnen muss. Button-Beschriftung ("Weiter wie bisher versuchen"/"Gewicht reduzieren (Empfehlung)") nur für `persistent_failure` überschrieben, generische Beschriftung für overload/consistencyGap unverändert gelassen (keine ungewollte Nebenwirkung auf bestehende Signale). Verifiziert: "Empfehlung folgen" setzt `nextWeekPlan` korrekt (-20 bei 80kg/deloadFactor 0.75) + zeigt Toast mit Übungsname+Zielgewicht; "Weiter wie bisher" loggt nur in decisionLog, `nextWeekPlan` bleibt 0, eigener Toast-Text. Regressionstest 10/10 grün. |
| B28 | Kein automatischer CI-Test bei Push | v162 | 6b6a7af | GitHub Actions (`.github/workflows/test.yml`) + Playwright (`playwright.config.js`, `tests/regression_core.spec.js`, `tests/fixtures.spec.js`) eingerichtet. **ID-Hinweis:** Sprint-Vorgabe nannte "B27" — bereits an anderer Stelle in dieser Session für das zurückgestellte dragdrop.js-Touch-Drag-Problem vergeben (siehe OFFEN-Tabelle), daher hier B28 verwendet. **Abweichungen von der Sprint-Vorlage** (nach Prüfung des echten DOM, wie explizit gefordert): (1) `regression_core.html` schreibt sein Ergebnis als Klartext in `<pre id="result">`, nicht als Elemente mit `.test-result`/`.pass`-Klassen wie in der Vorlage angenommen — Spec liest stattdessen den Textinhalt und prüft auf "bestanden"/"Gesamt uncaught errors während des Laufs: 0". (2) `npx playwright install --with-deps chromium` statt ohne `--with-deps` — ubuntu-latest-Runner braucht die Systembibliotheken, sonst startet Chromium dort nicht. (3) `pageerror`-Listener in `fixtures.spec.js` wird VOR jeder Navigation registriert (Vorlage registrierte ihn erst nach dem Reload — hätte Fehler während des Reloads selbst verpasst). (4) `npm init -y` + manueller "Start local server"-Schritt aus der Workflow-Vorlage entfernt — stattdessen committetes `package.json` (mit `@playwright/test` + `serve` als devDependencies) und `playwright.config.js`s eigener `webServer`-Block, der den Server automatisch startet und auf Bereitschaft wartet (robuster als ein fixes `sleep 2`). **Update 2026-07-13:** Node.js v24.18.0 LTS installiert (winget) — lokaler Playwright-Pre-Check (Loop 1, LOOPS.md) läuft jetzt tatsächlich, 16/16 grün bestätigt. Einmaliger Flake direkt nach Chromium-Erstinstallation (Kaltstart) trat auf, 4 Wiederholungen danach durchgehend sauber — siehe LOOPS.md für Details, nicht als echte Race Condition eingestuft. **Wichtig:** der Workflow blockiert den Push selbst NICHT (kein Branch-Protection-Gate eingerichtet, da main direkt ohne PR-Flow gepusht wird) — er liefert nur ein sichtbares Fehlersignal danach (Badge in README.md). |
| B18 | Meter statt Wdh fehlt als Progressionstyp | v165 | 11eb62e | Diagnose ergab: `metric:'m'/'sec'` (Distanz/Zeit) war bereits vollständig UI-verdrahtet (Eingabefelder, Einheiten-Labels) — die eigentliche Lücke lag in `getWeightRecommendation()` (weightRecommendation.js), das über den `lastWeight<=0`-Guard für jede Distanz/Zeit-Übung immer `null` lieferte (dort ist `s.weight` nie gesetzt), wodurch das New-Week-Modal für diese Übungen nie eine Steigerungsempfehlung zeigte. Design vorab mit Nutzer besprochen (3 Fragen: Scope 'm'+'sec' vs. nur 'm', feste vs. konfigurierbare Schrittweite, gleiche vs. eigene Auto-Vorauswahl-Schwelle vor Implementierung). **Umsetzung:** (1) `weightRecommendation.js`: gemeinsame Entscheidungslogik in `_recommendationCore()` extrahiert, neue `getMetricRecommendation()` liest `s.reps` (trägt bei diesen Metriken die Distanz/Zeit) statt `s.weight` — bewusst MIT denselben Feldnamen (`recommendedWeight`/`lastWeight`) im Rückgabewert wie `getWeightRecommendation()`, damit sämtlicher Verbrauchercode in ui.js beide Ergebnisse identisch behandeln kann. `getWeightRecommendation()`s Originalverhalten bewusst exakt erhalten (fixe Deltas 2.5/1.25, unabhängig von plateStep) — per Test abgesichert, nicht versehentlich an den Rundungsschritt gekoppelt. (2) Neues Feld `ex.metricStep` (Analogon zu `ex.weightStep`, User-Entscheidung: konfigurierbar statt fest), neue Action `EX_SET_METRIC_STEP`, neuer Schrittweite-Picker in der Übungskonfiguration (Distanz: 10/25/50/100/200m, Zeit: 5/10/15/30/60s). (3) `ex.progressionType`-Default korrigiert: bei `metric!=='reps'` ist `'weight'` bedeutungslos (kein Gewicht getrackt) — neuer Default `'reps'` (bumpt `targetReps` = Ziel-Distanz/-Zeit über den bereits bestehenden `_applyPlannedProgression()`-Pfad, keine Änderung dort nötig) an allen Erstellungsstellen (EX_ADD, Urlaubspläne, Onboarding-Templates) + Migration v29→v30 für bestehende Übungen mit dem alten, unveränderten Default. (4) New-Week-Modal-Logik (ui.js) branch't jetzt nach `ex.metric` zwischen `getWeightRecommendation`/`getMetricRecommendation`; dabei einen Blocker im bestehenden Skip-Guard gefunden und korrigiert (`if (progressionType==='reps') return;` hätte mit dem neuen Default JEDE Distanz/Zeit-Übung übersprungen, bevor sie überhaupt geprüft wird — Guard jetzt nur noch für `metric:'reps'`-Übungen mit `progressionType:'reps'`, dem ursprünglich gemeinten Fall). (5) Empfehlungs-Chip (`_renderRecChip`) + manueller "+"-Button + Schnellwahl-Picker + Bestätigungs-Toast alle metrikabhängig beschriftet ("m"/"Sek" statt "kg"). **Nebenbefund (nicht gefixt, nur dokumentiert):** `_renderAnalysis1RM()` (ui.js:2426) hat einen vermutlich immer-wahren Guard (`ex.metric !== 'kg'`, 'kg' ist nie ein gültiger Metrik-Wert) — neu als B31 in OFFEN getrackt. Verifiziert: dedizierte Test-Harness (16 Checks: getMetricRecommendation-Kaskade, getWeightRecommendation-Regressionsschutz bei nicht-Standard plateStep, EX_ADD-Defaults für alle 3 Metriken, Migration) + End-to-End-UI-Test (New-Week-Modal zeigt "+50m → 450m"-Chip, automatisch vorausgewählt, targetReps nach Wochenerstellung korrekt erhöht statt Gewicht) — alle grün, 0 uncaught errors. Neue Fixture `TRAIN_Test_EdgeCase_DistanceProgression.v1.json`. Regressionstest 10/10 grün, Playwright lokal 18/18 grün. |
| B34 | Lighthouse `aria-allowed-role`: `<main id="page-workout" role="tabpanel">` — `role="tabpanel"` ist für `<main>` kein zulässiger ARIA-Wert | v168 | fe71d80 | Gefunden bei B33 (v167), damals außerhalb des index.html/styles.css-Scopes zurückgestellt. Fix: `<main id="page-workout">` → `<section id="page-workout">` in `_buildScaffold()` (ui.js) — analog zu den anderen 4 Tab-Seiten, die bereits `<section role="tabpanel">` verwenden. Kein CSS/JS referenziert das Element über den Tag-Namen `main` (per Grep bestätigt), nur über `#page-workout` — Änderung risikofrei. Verifiziert: Lighthouse Accessibility 95 → 100 (zusammen mit B35-Fix), Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B35 | Lighthouse `aria-prohibited-attr`: `<div id="days-container" aria-label="Trainingstage">` — `aria-label` auf `<div>` ohne gültige Rolle ist nach ARIA-in-HTML nicht erlaubt | v168 | fe71d80 | Gefunden bei B33 (v167), damals zurückgestellt. Fix: `role="region"` auf dem Element ergänzt — macht `aria-label` semantisch gültig, kein Verhaltens-/Styling-Einfluss. Verifiziert zusammen mit B34: Lighthouse Accessibility 95 → 100, Regressionstest 10/10 grün, Playwright 18/18 grün. |
| B32 | Push/Pull-Ratio-Block zählt nur success-Sätze statt success+fail (letzter Nebenfund aus B22) | v167 | — | War der einzige der 4 Erfolgsquote-Stellen im UI, der seit B22 (v157) noch nicht auf success+fail vereinheitlicht war — in HANDOFF.md v166 als offener Nebenfund notiert. Fix: `_renderMovementPattern()` (ui.js, Push/Pull-Ratio-Block) — `ex.sets.filter(s => s.status === 'success')` → `filter(s => s.status === 'success' \|\| s.status === 'fail')`, identisch zum bereits vereinheitlichten Muster der anderen 3 Stellen. `_weekSuccessScore()`/`_weekTrainingStatus()` bewusst unverändert (andere Semantik: Anwesenheit statt Zielerreichung, siehe DECISIONS.md/CLAUDE.md "Conceptual Framework"). Verifiziert: Regressionstest 10/10 grün, Playwright 18/18 grün nach dem Fix (kein Fixture testet den Push/Pull-Ratio-Text isoliert, aber kein bestehender Test zeigte eine Regression). |
| B33 | Lighthouse Accessibility 91 → 95: Farbkontrast `--c-text-3` unter 4.5:1 | v167 | — | Lighthouse-Report (`.lighthouseci/lhr-*.json`, lokal erzeugt via `npx lhci autorun`) zeigte 3 Accessibility-Findings unter Score 1: `color-contrast` (3 Elemente, `.ob-tpl-sub` u.a. — Kontrast 3.63 statt geforderter 4.5:1, Ursache: `--c-text-3: #72727A` gegen `--c-surface: #1A1A1E`), sowie `aria-allowed-role`/`aria-prohibited-attr` (siehe B34/B35 — beide außerhalb des index.html/styles.css-Scopes dieses Sprints, damals nicht gefixt, **seit v168 behoben**). Nur der Kontrast-Fund war im erlaubten Scope eindeutig behebbar: `--c-text-3` global auf `#90909A` angehoben (styles.css) — berechnet gegen alle 3 Hintergründe, auf denen die Variable tatsächlich verwendet wird (`--c-surface` 1A1A1E: 5.34, `--c-surface2` 242428: 4.76, `--c-bg` 0E0E10: 5.93 — alle ≥4.5:1 mit Marge). Bewusst die CSS-Variable global geändert statt nur `.ob-tpl-sub` lokal zu überschreiben, da `--c-text-3` an >100 weiteren Stellen im UI verwendet wird und derselbe Kontrastfehler dort ebenso vorläge, auch wenn Lighthouse (nur ein DOM-Zustand pro Lauf) sie in diesem Durchlauf nicht erfasst hat. Ergebnis lokal verifiziert: Accessibility-Score 91 → 95 (2 von 3 `lhci autorun`-Läufen erfolgreich — 1 Lauf schlug mit dem bekannten Windows-EPERM-Cleanup-Fehler fehl, siehe B30, kein neues Problem). Regressionstest 10/10 grün, Playwright 18/18 grün danach — reine CSS-Variablenwert-Änderung, keine Klassennamen/Selektoren geändert. |
| B31 | `_renderAnalysis1RM()`-Fallback zeigt nie ein 1RM, wenn `state.prs` noch keinen Eintrag hat (v.a. bei Ausweichübungen) | v166 | 66455e0 | Vorab vollständig diagnostiziert (siehe Diagnose-Log dieser Session) — Root Cause: `ui.js:2426` `if (ex.metric && ex.metric !== 'kg') return [];`, 'kg' ist nie ein gültiger `ex.metric`-Wert, Bedingung damit praktisch immer wahr, Fallback lieferte immer `[]`. **Fix:** zu `!== 'reps'` geändert (1RM-Schätzung ergibt nur für gewichtsgetrackte Übungen Sinn) + Kommentar mit Begründung ergänzt. Verifiziert: (1) `TRAIN_Test_EdgeCase_MaxGewicht.v1.json` (leeres `prs`) zeigt jetzt korrekt "~550.0 kg geschätzter 1RM (Epley · 500 kg × 3 Wdh)" im Fortschritt-Tab. (2) Ausweichübungs-Szenario (eigener Test: echte Kniebeuge-Session 100kg×5 + spätere Beinpresse-Substitution 200kg×8) zeigt jetzt korrekt das höhere Epley-Ergebnis (253.3) aus der Substitutions-Woche — genau der Fall, für den der Fallback ursprünglich gebaut wurde. (3) Regressionsschutz: metric 'm'/'sec'-Übungen (DistanceProgression-Fixture) zeigen weiterhin korrekt KEINEN 1RM-Hint (kein Gewicht getrackt). Alle 3 Szenarien 0 uncaught errors. Regressionstest 10/10 grün (2 isolierte Flakes bei Prüfpunkt 8 währenddessen beobachtet, bekanntes Headless-Timing-Artefakt, unabhängig bestätigt via Playwright 18/18 grün — die Funktion, die B31 betrifft, hat nichts mit Tag-Navigation zu tun). |
| B29 | Mehr-Übungen-Aggregation fehlte für persistent_failure | v163 | 221da35 | Design mit Nutzer besprochen (3 Fragen, siehe DECISIONS.md) bevor implementiert (CLAUDE.md-Pflichtfragen bei konzeptionell neuen Features). Neue Funktion `_checkMultiExerciseFailure()` (weeklyFocus.js) in `computeStructuralSignals()` eingehängt (Strukturkarte, NICHT akute Kaskade) — erkennt verteiltes Scheitern über ≥2 Übungen (Gesamterfolgsquote ≤20% über die letzten 3 Nicht-Deload-Wochen, mind. 15 bewertete Sätze, jede betroffene Übung mit ≥2 Datenpunkten), als Ergänzung zu `_checkPersistentFailure` (dort: EINE Übung bei 0%). Reiner Informationstext in der Strukturkarte (2-3 am schlechtesten performende Übungen + je eine Gewichtsempfehlung über `deloadFactor`+`roundToPlate()`) — bewusst KEIN Aktions-Button/Decisional-Balance, hält die bestehende "Strukturkarte = rein informativ"-Konvention ein. Priorität innerhalb der Strukturkarte: zuoberst (vor Präventiver Deload), da der konkreteste Befund unter den strukturellen Signalen. Neue Fixture `tests/fixtures/TRAIN_Test_EdgeCase_MultiExerciseFailure.v1.json` erstellt und headless verifiziert: 3 Übungen mit je 17% Erfolgsquote (bewusst nicht 0%, damit KEINE Übung einzeln `_checkPersistentFailure` auslöst — isolierter Test der neuen Aggregation) → Strukturkarte zeigt korrekt `multi_exercise_failure` mit allen 3 Übungen + Gewichtsempfehlungen, Hauptkarte bleibt beim Fallback "Auf Kurs" (kein Overlap, wie designt). Nebenbei zwei stale Doku-Drifts in weeklyFocus.js-Kopfkommentar behoben (akute Kaskade listete noch die alte 6-Punkte-Nummerierung ohne persistent_failure von v160). Regressionstest 10/10 grün (1 isolierter Flake bei Prüfpunkt 8 unterwegs beobachtet, 3 weitere Läufe danach sauber — als bekanntes Headless-Timing-Artefakt eingeordnet, siehe LOOPS.md/B28, kein Zusammenhang mit diesem Fix). Playwright lokal: 17/17 grün. |
| B30 | Kein Lighthouse CI (Performance/Accessibility nicht automatisch geprüft) | v164 | d1241a6 | **ID/Versions-Hinweis:** Sprint-Vorgabe nannte "B28"/"train-v163" — beide bereits an anderer Stelle vergeben (B28 = GitHub Actions CI, train-v163 = B29 Mehr-Übungen-Aggregation), daher hier B30/train-v164 verwendet. Zweiter GitHub-Actions-Job `lighthouse` (needs: regression) in `.github/workflows/test.yml` ergänzt, `lighthouserc.cjs` neu (bewusst `.cjs` statt `.js` — package.json hat `"type":"module"`, macht alle `.js`-Dateien zu ES-Modulen, `@lhci/cli` lädt die Config aber per `require()`/CommonJS; mit `.js`+`export default` scheiterte `lhci autorun` lokal mit "Unable to automatically determine staticDistDir", da die Config gar nicht geladen wurde — mit `.cjs`+`module.exports` funktioniert es). `@lhci/cli` als devDependency in package.json statt globalem `npm install -g` (Konsistenz mit Playwright/serve-Ansatz aus B28). `collect.startServerCommand` statt separatem manuellen "Start server"-CI-Step — LHCI startet den Server selbst und wartet auf Erreichbarkeit (gleiche Race-Condition-Klasse wie bei B28 vermieden, dort mit Playwright's `webServer` gelöst). Lokal getestete echte Scores: Performance 84, Accessibility 91 (klar über der blockierenden 0.8-Schwelle), Best Practices 96, SEO 100 (nicht Teil der Assertions). **Keine `categories:pwa`-Assertion** — real getestet und festgestellt, dass diese Lighthouse-Version keine PWA-Kategorie mehr hat (seit Lighthouse v9 aus den Default-Kategorien entfernt), die Assertion hätte IMMER mit "auditRan: 0" gewarnt, unabhängig von echter PWA-Qualität — bewusst weggelassen statt bedeutungsloses Dauer-Warnen zu behalten. **Bekannte lokale Einschränkung:** `npx lhci autorun` schlägt auf dieser Windows-Maschine in ca. 4 von 5 Läufen mit einem `EPERM`-Fehler beim Cleanup des Chrome-Temp-Profils fehl (chrome-launcher-Eigenart unter Windows, tritt NACH erfolgreicher Messung auf, betrifft nur lokale Windows-Entwicklung — GitHub Actions läuft auf `ubuntu-latest`, wo dieses Problem nicht bekannt ist). |

---

## OFFEN

| ID | Beschreibung | Priorität | Notizen |
|----|-------------|-----------|---------|
| B41 | Plateau-Strategie "Variation" ist strukturell nie erreichbar | Low (bewusst nur dokumentiert) | Gefunden im Deep-Check-Audit (v169). `_hasSharedMuscleGroupDay()` (plateauDetector.js) hängt vollständig von `ex.tags` ab — dieses Feld wird nirgends im UI befüllt (bereits von insightEngine.js selbst so dokumentiert: "ex.tags wird nirgends im UI befüllt (immer [])"). Damit liefert die Funktion immer `false`, und `detectPlateaus()` kann die `'variation'`-Strategie nie wählen — jedes Plateau landet nur bei `'deload'` oder `'volume'`. Kein Crash, kein Datenverlust — ein ganzer, aber unauffälliger Feature-Zweig ist tot. Mit Nutzer besprochen: bewusst NICHT behoben (bräuchte entweder eine neue UI zum Pflegen von `ex.tags` — größerer Aufwand — oder die ersatzlose Entfernung der `'variation'`-Option, was echten Funktionsverlust bedeutet), nur dokumentiert für eine spätere bewusste Entscheidung. |
| B42 | `lastBackupDate` wird an zwei Stellen mit unterschiedlichem Typ geschrieben (Zahl vs. Datums-String) | Low | Gefunden im Deep-Check-Audit (v169). `backup.js:48` schreibt `Date.now()` (Zahl), `backup.js:258` schreibt `today()` ("YYYY-MM-DD"-String). Der einzige Lesepfad (`_backupAgeInDays()`, ui.js) behandelt beide Formate bereits defensiv korrekt — funktioniert heute richtig, ist aber fragil bei einer künftigen dritten Schreibstelle. Nicht akut, nur zur Kenntnis. |
| B43 | `CUSTOM_EX_DELETE` räumt keine Bewegungskategorie-Zuordnung für historische Wochen auf | Low | Gefunden im Deep-Check-Audit (v169). Löscht nur den `customExercises`-Eintrag; bereits geloggte Sätze bleiben unangetastet (kein Datenverlust), aber die Übung fällt in Bewegungsmuster-/Push-Pull-Auswertungen rückwirkend auf "Sonstige" zurück, sobald ihre Kategorie-Definition gelöscht wird. Vertretbares Verhalten, nur als Beobachtung dokumentiert. |
| B19 | Outlook/Prognose bei konstantem Gewicht | Low | curRate ≤ 0 → kein Korridor. "Kein klarer Trend" erscheint korrekt (by design) |
| B20 | getCompletionRate()/_scoreWeek() isFullSuccess | Low | Verwandter Bug-Typ. Bewusst zurückgestellt (Semantik: Anwesenheit, nicht Ziel) |
| B21 | Sperrbildschirm-Integration | UX-komplex | Media Session API + Service Worker. Nach echten Nutzern |
| B27 | Touch-Drag-Reorder funktioniert trotz aktiviertem Polyfill (B24) nicht auf echtem Gerät | Low (zurückgestellt) | Per Nutzer-Test 2026-07-13 bestätigt: Polyfill lädt fehlerfrei (headless bestätigt), Reorder selbst greift auf echtem Gerät aber nicht. Bewusst NICHT priorisiert — Übungsreihenfolge ist bereits über die Pfeil-Buttons in den Übungseinstellungen änderbar, das deckt den eigentlichen Bedarf bereits ab. Tiefere Diagnose (holdToDrag-Timing? dragstart feuert nicht auf Touch? MobileDragDrop-Version?) auf später verschoben. **Update 2026-07-14 (Pre-Launch-Checkliste):** im Rahmen der Launch-Vorbereitung erneut geprüft und bewusst bestätigt — kein Blocker für den Launch an die ersten ~20 Nutzer, da die Pfeil-Buttons den Bedarf abdecken. |
| B55 | Impressum-Platzhalter ist ein akutes Abmahnrisiko (nicht nur ein TODO) | **Blocker vor weiterer Nutzerwerbung** | DSGVO/Rechts-Review 2026-07-14 (reine Doku-Session, kein Code geändert). `ui.js:4113-4118` zeigt aktuell nur "TODO: vor Veröffentlichung ausfüllen" statt Name/Anschrift/Kontakt. App läuft bereits live unter github.io/TRAIN, Nutzerwerbung (Reddit) und eine Bezahlfunktion sind laut CLAUDE.md/HANDOFF.md geplant — reicht nach deutscher Rechtsprechung für "geschäftsmäßiges Angebot" i.S.d. § 5 TMG/DDG bzw. § 18 MStV. Fehlendes Impressum ist einer der am leichtesten automatisiert auffindbaren Abmahngründe. War als offenes TODO bereits in HANDOFF.md gelistet, aber ohne Rechts-Einordnung/Priorität — jetzt explizit als Blocker getrackt. Braucht echte Angaben vom Nutzer (Name + ladungsfähige Anschrift), kann nicht durch Code allein gelöst werden. Hängt zusammen mit der Platzhalter-Feedback-Mail (`ui.js:4090`, `mailto:DEINE-EMAIL@beispiel.de`). |
| B56 | Datenschutz-Akkordeon ist keine vollständige Art.-13-DSGVO-Erklärung | Mittel | Gleiches Review. `ui.js:4097-4106` ist inhaltlich korrekt, aber unvollständig: fehlt Verantwortlicher (hängt an B55), Rechtsgrundlage (Art. 6 Abs. 1 lit. f), Betroffenenrechte (Auskunft/Löschung/Widerspruch/Beschwerde bei Aufsichtsbehörde), und v.a. ein Hosting-Hinweis — GitHub Pages (Microsoft) verarbeitet beim Ausliefern der Seite serverseitig IP-Adressen aller Besucher, unabhängig von GoatCounter, fehlt aber komplett im Text. Formulierung "ohne personenbezogene Daten" bei GoatCounter zu absolut (IP gilt auch gehasht/pseudonymisiert grundsätzlich als personenbezogen) — sollte präzisiert werden. Text-Fix, sobald B55 die Verantwortlicher-Angaben liefert. |
| B57 | Keine "Alle Daten löschen"-Funktion in den Einstellungen | Low | Gleiches Review. Nutzer muss aktuell Browser-Storage manuell leeren, um lokale Daten zu entfernen — kein zwingender DSGVO-Verstoß (Daten verlassen nie das Gerät, Betreiber hat keinen Zugriff), aber vertrauensbildend und leicht nachrüstbar (ein Button → `localStorage.removeItem('train_v6')` + Shadow-Key + Reload). |
| B58 | OFL.txt (Font-Lizenztext) fehlt im `fonts/`-Ordner | Low | Gleiches Review. Bebas Neue + DM Sans (B51, selbst gehostet) stehen unter SIL Open Font License — rechtlich unproblematisch, aber OFL verlangt bei Weiterverteilung eigentlich Mitlieferung des Lizenztexts. Kleinigkeit, schnell ergänzt. |

---

## BEWUSST KEIN BUG (by design)

| Beschreibung | Begründung |
|-------------|-----------|
| Pull Ups (weight=0) löst _checkRisingRpe nicht aus | Guard ist korrekt — Körpergewicht-Übungen haben kein messbares Gewicht für den Overload-Check |
| _scoreWeek() nutzt kein isFullSuccess() | Misst Anwesenheit ("hat trainiert"), nicht Zielerreichung. Semantisch korrekt für Streak/Konsistenz |
| Prognose fehlt bei stagnierenden Übungen | curRate ≤ 0 → kein Trend → keine Prognose. "Kein klarer Trend" ist die korrekte Aussage |
| Konfidenz HIGH/MEDIUM/LOW und Progressionsbereitschaft haben unterschiedliche RPE-Schwellen | Messen verschiedene Dinge — nicht vereinheitlichen |
| Wochenrückblick-Karte zeigt "1/2 Tage" UND "100% Ziel" für dieselbe Woche gleichzeitig | Gefunden bei der echten Geräte-Verifikation (2026-07-14) von B38 (Urlaubstag-Konsistenz-Fix, train-v169). Sieht auf den ersten Blick widersprüchlich aus, ist aber zwei unabhängige, je für sich korrekte Kennzahlen: "1/2 Tage" = `completedDays`/`plannedDays` (weekReview.js) misst Tage-Anwesenheit; "100% Ziel" = `goalFulfillment`/`_calcSuccessScore()` (weekReview.js) misst die Erfolgsquote NUR der tatsächlich bewerteten Sätze (unabhängig davon, ob alle geplanten Tage absolviert wurden). Im getesteten Fall: 1 von 2 Tagen erledigt (Urlaubstag korrekt NICHT mitgezählt, B38 bestätigt funktionierend), aber die Sätze am erledigten Tag waren zu 100% erfolgreich — beide Zahlen stimmen, messen aber unterschiedliche Dinge. Label bewusst unverändert gelassen (Nutzer-Entscheidung) — siehe B44/B45 für die zugehörigen technischen Funde, beide seit v170 konsolidiert/behoben. |

---

## BEKANNTE FALLSTRICKE (für Test-JSON-Konstruktion)

| Fallstrick | Wirkung | Lösung |
|-----------|---------|--------|
| weight=0 in Übung | _checkRisingRpe überspringt Übung | Echtes Gewicht > 0 verwenden |
| lastReentryHandled gesetzt | _checkReentry feuert immer (Prio 1) | null setzen |
| Zu viele Wochen (>8) | Präventiver Deload feuert vor Push/Pull | Max 7 Wochen für Push/Pull-Tests |
| fail-Sätze fehlen | _checkConsistencyQuality feuert nicht | Echte fail-Sätze nötig (reps<targetReps reicht nicht) |
| Gewicht steigt konstant | Plateau-Signal verdrängt Push/Pull | Konstantes Gewicht ODER <3 Wochen für Plateau-Bedingung |
| Übungsname nicht in movementMap | pullSets=0 → Push/Pull-Guard greift | Bekannten Namen verwenden (z.B. "Rudern" statt "Barbell Row") |
| curPct < 0.7 als Guard | Totes Code — Scale ist 0-100 | curPct < 70 verwenden |
| Genau 2 Wochen identisches Gewicht | Kein Plateau (braucht 3+, verifiziert v160) | 3+ Wochen für Plateau-Tests verwenden |
| Genau 8 Wochen ohne Deload | Präventiver Deload feuert (Schwelle ≥8, verifiziert v160) | Für Push/Pull-Tests weiterhin max. 7 Wochen (siehe oben) |
